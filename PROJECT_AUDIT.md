# 项目静态审查报告

本报告基于当前仓库内容进行静态检查，重点关注 PC 侧跟随程序与固件代码的潜在问题与疏漏。

## 发现的问题

1. **YOLO 权重路径硬编码且仓库缺失对应文件**：`PC_YOLO_TEST/pc_test.py` 与 `PC_YOLO_TEST/get_item_code.py` 均直接加载 `yolov8s.pt`，但仓库仅提供 `yolov8s.onnx`，在新环境启动时会因权重文件不存在而立即报错，需要改为可配置并提供占位符或将 ONNX 推理接入。【F:PC_YOLO_TEST/pc_test.py†L18-L19】【F:PC_YOLO_TEST/get_item_code.py†L3-L3】
2. **串口资源未在退出前关闭**：`start_recognition` 的主循环跳出后只释放摄像头资源，未显式调用 `ser.close()`，如果后续进程/线程复用串口可能导致端口被占用或缓冲区未刷写。【F:PC_YOLO_TEST/pc_test.py†L162-L163】
3. **调试变量未使用**：`dead_zone` 在 PID 纠偏逻辑里被定义但完全未使用，可能是漏掉了静区判断逻辑，导致车辆在微小误差时仍频繁调整；应明确删除或补充对应判断。【F:PC_YOLO_TEST/pc_test.py†L120-L123】
4. **辅助脚本缺乏可执行入口与异常处理**：`export_onnx.py`、`get_item_code.py` 直接在导入后运行推理/导出操作，未做 `__main__` 保护也未处理模型文件不存在等错误，稍有改动即可能在导入时异常中断开发环境。【F:PC_YOLO_TEST/export_onnx.py†L1-L6】【F:PC_YOLO_TEST/get_item_code.py†L1-L5】

## 建议

- 提供统一的配置方式（命令行/配置文件/环境变量）选择权重格式与路径，并在缺失时给出清晰提示或默认占位符。
- 在摄像头/串口退出流程中补充串口关闭与异常保护，确保资源释放可预期。
- 如果需要“静区”抑制小幅误差振荡，应将 `dead_zone` 纳入指令生成逻辑；否则删除以避免误导。
- 为辅助脚本添加 `if __name__ == "__main__":` 保护，并在无法找到权重或导出失败时输出中文错误信息，避免意外执行。

